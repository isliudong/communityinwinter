<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.liudong.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="life.liudong.community.model.Question">

        <id column="ID" jdbcType="INTEGER" property="id"/>
        <result column="TITLE" jdbcType="VARCHAR" property="title"/>
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified"/>
        <result column="CREATOR" jdbcType="INTEGER" property="creator"/>
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount"/>
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount"/>
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount"/>
        <result column="TAG" jdbcType="VARCHAR" property="tag"/>
        <result column="IMG_URL" jdbcType="VARCHAR" property="imgUrl"/>
        <result column="TYPE" jdbcType="VARCHAR" property="type"/>
        <result column="DESCRIPTION" jdbcType="VARCHAR" property="description" />
    </resultMap>
    <insert id="insertAndReturnId" useGeneratedKeys="true" keyProperty="id" parameterType="life.liudong.community.model.Question">
      INSERT INTO
         QUESTION(title,gmt_create,gmt_modified,creator,comment_count,view_count,like_count,tag,description,img_url,type)
      values (#{title},#{gmtCreate},#{gmtModified},#{creator},#{commentCount},#{viewCount},#{likeCount},#{tag},#{description},#{imgUrl},#{type});
    </insert>

    <update id="incView" parameterType="life.liudong.community.model.Question">
    update QUESTION
    set
    VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
    where id=#{id}
  </update>

    <update id="incCommentCount" parameterType="life.liudong.community.model.Question">
    update QUESTION
    set
    COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where id=#{id}
  </update>

    <select id="selectRelated" parameterType="life.liudong.community.model.Question" resultMap="BaseResultMap">
    select * from QUESTION
    where id != #{id} and tag regexp #{tag}
  </select>

    <!--搜索查询问题数量语句-->
    <select id="countBySearch" parameterType="life.liudong.community.dto.QuestionQueryDTO"
            resultType="java.lang.Integer">
        select count(*) from QUESTION
        <where>
            <if test="search !=null">
                and title regexp #{search}
            </if>
            <if test="tag !=null">
                and tag regexp #{tag}
            </if>
            <if test="type !=null">
                and type regexp #{type}
            </if>
        </where>
    </select>


    <!--搜索问题-->
    <select id="selectBySearch" parameterType="life.liudong.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
        select * from QUESTION
        <where>
            <if test="search !=null">
                and title regexp #{search}
            </if>
            <if test="tag !=null">
                and tag regexp #{tag}
            </if>
            <if test="type !=null">
                and type regexp #{type}
            </if>
        </where>
        order by gmt_create desc limit #{page},#{size}
    </select>


</mapper>